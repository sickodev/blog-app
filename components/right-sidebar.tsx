import { cn } from "@/lib/utils";
import React from "react";

const RightSidebar = ({ classname }: { classname?: string }) => {
    return <div className={cn("", classname)}>RightSidebar</div>;
};

export default RightSidebar;
